export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Links {
  title: string;
  caption: string;
  icon: string;
  link: string;
}
